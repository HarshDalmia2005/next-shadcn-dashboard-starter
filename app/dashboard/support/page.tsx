import { CustomerSupport } from '@/sections/support/view';

export const metadata = {
  title: 'Dashboard : Customer Support Page'
};

export default function page() {
  return <CustomerSupport />;
}
